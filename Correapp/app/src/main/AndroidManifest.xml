<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fiuba.tdpii.correapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.TestPhotoUploadActivity"></activity>
        <activity android:name=".activities.TripTooManyRejectsActivity" />
        <activity android:name=".activities.StartTripDriverActivity" />
        <activity android:name=".activities.ClientDriverProfileActivity" />
        <activity android:name=".activities.DriverProfileActivity" />
        <activity android:name=".activities.CreateDriverCarActivity" />
        <activity android:name=".activities.CreateDriverActivity" />
        <activity android:name=".activities.FbLoginActivity" />
        <activity android:name=".activities.ChoferSeguimiento" />
        <activity android:name=".activities.RechazarActivity" />
        <activity android:name=".activities.ChoferViewTripActivity" />
        <activity android:name=".activities.ChoferActivity" />
        <activity android:name=".activities.RateTripDriverActivity" />
        <activity android:name=".activities.SeguimientoActivity" />
        <activity android:name=".activities.WaitingActivity" />
        <activity android:name=".activities.TripsActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.AddPetActivity" />
        <activity android:name=".activities.CreateTripActivity" />
        <activity android:name=".activities.RateTripClientActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.ShowRouteActivity"
            android:label="@string/title_activity_show_route" />
        <activity android:name=".activities.DestinyMapActivity" />
        <activity
            android:name=".activities.LogoActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_logo"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MapActivity" />
        <activity android:name=".activities.MapHomeActivity" />

        <service
            android:name=".services.LocationService"
            android:enabled="true"
            android:exported="true" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
    </application>

</manifest>